#include <iostream>


int main(void)
{
std::string checkString;

checkString = " I read and right She felt so light and we will unite We will fight but again reunite coz u in my right and will be my wife ";

if (nullptr != checkString.c_str())
{
std::cout << checkString.c_str() << std::endl;
}

std::cout << " WARNING " << std::endl;

return (0);
}
